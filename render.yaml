services:
  - type: web
    name: goalscape-3d-viewer
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: false
      - key: NODE_VERSION
        value: 22
      - key: GOALSCAPE_MCP_URL
        sync: false
      - key: BACKEND_PUBLIC_URL
        sync: false
